<div class="card mb-3 postcard">
  <div class="card-body">
    <h5 class="card-title">{{post_title}}</h5>
    <p class="card-text">{{post_body}}</p>
    <a href="/users/{{user.user_name}}"class="card-text"><small class="text-muted">{{user.user_name}}</small></a>
  </div>
</div>