<div class="card mb-3 postcard">
  <div class="card-body">
    <h5 class="card-title">{{post_title}}</h5>
    <p class="card-text">{{post_body}}</p>
    <div class="bottompostcard">
        <a href="/users/{{poster_name}}"class="card-text"><small class="text-muted">{{poster_name}}</small></a>
        <a href="/comments/{{id}}"><i class="fas fa-comment"></i></a>
    </div>
  </div>
</div>